# V2 availability gates

V2 has no task PAUSED/DELETED recovery flow. The payment gateway has an intake-only pause and staking has a one-way activation gate. Their status does not change the task state enum. Check live status and use [failure modes](FAILURE_MODES.md). Historical V1 recovery guidance is non-normative.
