
  # --- AZZLE protocol (Base) — added by npx @azzle/agents aeon-setup ---
  azzle-market: { enabled: false, schedule: "0 8 * * *", market: standard, var: "" } # market is required: standard|micro
  azzle-worker: { enabled: false, schedule: "workflow_dispatch", market: standard, var: "" } # task refs must match market
